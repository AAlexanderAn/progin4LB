# Базовый образ с компилятором C++
FROM gcc:latest

# Создаём рабочую директорию
WORKDIR /app

# Копируем исходный код в контейнер
COPY . .

# Компилируем программу
RUN g++ -o myapp progin4LB.cpp

# Команда для запуска при старте контейнера
CMD ["./myapp"]